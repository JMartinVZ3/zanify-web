import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    CardContentStyle: {
        'textAlign':'center',
    },
    AddToCartButton:{
        'display': 'flex',
        'justifyContent': 'center',
        'textAlign': 'center'
    }
}));